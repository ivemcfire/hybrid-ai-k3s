Write a Python function `parse_sensor_csv(path: str) -> list[dict]` that reads a CSV with columns `timestamp,node_id,sensor,value`. Skip the header row. Return a list of dicts with keys `timestamp`, `node_id`, `sensor`, `value`. Convert `value` to float. Skip any row where `value` cannot be parsed as float. Use only the Python standard library. Output ONLY the function code in a single python code block, no explanation, no example usage.
